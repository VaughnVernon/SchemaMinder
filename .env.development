# Domo Schema Registry - Development Environment
# This file contains settings shared by all developers
# For personal overrides, create .env.local (which is git-ignored)

# ============================================================================
# API Configuration
# ============================================================================

# Backend API URL (Cloudflare Worker in dev mode)
API_URL=http://localhost:8789

# Frontend will use this for API calls
VITE_API_URL=http://localhost:8789

# ============================================================================
# Real-time Server Configuration
# ============================================================================

# PartyKit WebSocket server URL
PARTYKIT_URL=http://localhost:1999

# Frontend will use this for WebSocket connections
VITE_PARTYKIT_URL=http://localhost:1999

# ============================================================================
# Admin User (Initial Setup - Default Values)
# ============================================================================
# These are PLACEHOLDER values for documentation only
# NEVER commit real credentials to git!
# Set real values in .env.local (git-ignored)

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMe123!
ADMIN_FULL_NAME=Administrator

# ============================================================================
# Optional: Debug Configuration
# ============================================================================

# Enable debug logging (set to 'true' to enable)
# DEBUG=false

# Log level (error, warn, info, debug, trace)
# LOG_LEVEL=info
